{
  "name": "pefi",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start-dev": "concurrently \"npm run client-vite\" \"npm run server\"",
    "server": "node server/server.js",
    "install": "cd server && npm install",
    "client-vite": "cd client && npm run dev",
    "client-build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "eslint": "^8.48.0"
  }
}
