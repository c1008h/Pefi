export const selectNetworth = (state) => state.transaction.networth;
export const selectCash = (state) => state.transaction.cash;
export const selectDigital = (state) => state.transaction.digital;
export const selectSaved = (state) => state.transaction.saved;
export const selectInvested = (state) => state.transaction.invested;
